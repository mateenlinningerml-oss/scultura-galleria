# Version 1.0.4 — Production parity

- Render content.json is replaced by the repository version on deploy.
- Render uploads are mirrored exactly from the repository.
- Upload image files are included in Git instead of ignored.
- HTML, CSS, JavaScript and API responses disable stale caching.
