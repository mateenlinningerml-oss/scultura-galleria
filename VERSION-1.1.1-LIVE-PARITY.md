# Version 1.1.1 — Live parity fix

This release forces the repository `data/content.json` and bundled upload images onto Render's Persistent Disk once for version 1.1.1, so production matches localhost.
