# Version 1.2.2 — Live = Localhost

- Bumps the repository release marker so Render refreshes persistent content.
- Copies `data/content.json` from the repository to the persistent disk.
- Mirrors repository upload images exactly and removes stale production-only image files.
- Tracks website image files in Git so Render receives the same media as localhost.
