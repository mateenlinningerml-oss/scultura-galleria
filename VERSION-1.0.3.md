# Version 1.0.3 — Production Sync

Render synchronisiert bei diesem Release die im Repository enthaltene `data/content.json` und die mitgelieferten Uploads einmalig auf den Persistent Disk. Dadurch verwendet die Live-Website denselben Inhalt wie der getestete Localhost-Stand.
